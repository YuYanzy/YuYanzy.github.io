<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Docker Data Manage</title>
    <url>/2021/05/06/Docker%E6%95%B0%E6%8D%AE%E7%AE%A1%E7%90%86%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<p>在Docker Container中 Data有两种存在的状态。</p>
<p>状态一： 写入容器的数据</p>
<p>一种Data是直接存在Docker container的一个可写入层中，这种存储方式使得此种数据具有以下的特性：</p>
<ul>
<li>数据不能持久化，数据的生命周期与container的生命周期保持一致</li>
<li>数据只能存在container中，相当于数据与容器绑定死了。数据不能是远端的，也不能是其他容器中的。</li>
<li>相对性能较差（暂时不能理解）</li>
</ul>
<p>状态二： 容器外部的数据</p>
<p>另一种Data是存在容器外的，container能够访问到这种数据，在容器内进行正常的读写。对应的这种数据的好处如下：</p>
<ul>
<li>数据可以持久化，即便容器删除，数据依然可以保存。</li>
<li>数据与容器分离，数据甚至可以存在远端。多个容器可以访问同一种数据，实现容器之间数据流的协同。</li>
<li>方便维护管理，轻易复制。</li>
</ul>
<p>感觉有点像生物里面的 结构水和自由水的概念。</p>
<p>显然第二种数据是会更受大家喜欢的。</p>
<p>为了实现第二种数据的存储，有两种方式，分别是 volume 和 bind mounts。除此以外，在linux系统中可以使用tmpfs，在windows系统中可以使用 named pipe。（后面这两种我觉得应该不会用的很多）</p>
<p><a href="https://imgtu.com/i/glLTMR">
        <span class="lazyload-img-span">
        <img   
           data-src="https://z3.ax1x.com/2021/05/06/glLTMR.png" >
        </sapn>
      </a></p>
<p>首先，无论是volume 还是 bind mount， 文件在container中的存在方式，都是一个文件目录的形式。所以在docker 里面看到的都是文件夹·。tmpfs看起来是直接存在主机的内存里面的，并不是以硬盘上的文件形式存在。</p>
<p>既然最终的表现形式一样，那么volume和bind mount有什么区别呢？</p>
<p>先来看volume和Bind mount的定义有什么区别。</p>
<p>Volume 是存在 /var/lib/docker/volume 目录下的， 完全有Docker这个进程来管理，别的软件没有访问权限。</p>
<p>Bind Mount 在主机上的文件夹可以存在任何地方，甚至可以使重要的系统文件和目录。主机上非Docker的进程也可以随意访问修改它。</p>
<p>所以总结来说，这两种方式最大的区别在于它们的来源的区别。Volume相当于是遵守Docker内部规则的亲儿子，而bind mount相当于是外包。</p>
<p>再来仔细看一下Volume和Bind Mount分别有哪些细节上的差别。</p>
<p>Volume 的创建和管理都由Docker实现。</p>
<p>有两种创建方式。</p>
<ul>
<li>直接通过 docker volume create 命令来创建</li>
<li>Docker在创建conatiner或者service的流程中创建</li>
</ul>
<p>volume在创建之后，存在/var/lib/docker/container中。同一个volume可以同时被多个container mount。 没有container使用的volume不会自动删除。可以使用 docker volume prune 的方式删除没有被任何container使用的volume。</p>
<p>volume有 named 和 anonymous 两种， anonymous的volume会被随机编码命名。</p>
<p>volume支持使用 volume drivers （不懂）， 可以允许 把数据存放在远端。</p>
<p>Bind Mount 与volume相比，有些限制的地方。它的文件必须依赖主机操作系统的文件结构。（好像也没啥不好啊）。 也不能通过docker cli command直接管理 bind mounts。 推荐使用volume。</p>
<p>container可以通过bind mount这种方式，直接修改主机的文件，从而可以影响到主机的一些其他进程。这样好像一方面破坏了docker的隔离性，另外一方面又好像使得docker更加powerful了。也是个trade off吧。</p>
<p>volume 的 good use cases：</p>
<ul>
<li><p>在多个容器之间分享数据，还可以定制读写权限</p>
</li>
<li><p>把文件存储的形式和主机做隔离，不受主机结构的限制。</p>
</li>
<li><p>可以把数据存在远端</p>
</li>
<li><p>方便 备份 还原 迁移</p>
</li>
<li><p>volume存在linux虚拟机中，支持高I /O， 延迟低 When your application requires high-performance I/O on Docker Desktop. Volumes are stored in the Linux VM rather than the host, which means that the reads and writes have much lower latency and higher throughput.</p>
</li>
<li><p>When your application requires fully native file system behavior on Docker Desktop. For example, a database engine requires precise control over disk flushing to guarantee transaction durability. Volumes are stored in the Linux VM and can make these guarantees, whereas bind mounts are remoted to macOS or Windows, where the file systems behave slightly differently.</p>
<p>总结来说，还是volume的文件目录不受主机操作系统影响吧。</p>
</li>
</ul>
<p>bind mount的good use cases:</p>
<ul>
<li>Sharing configuration files from the host machine to containers. This is how Docker provides DNS resolution to containers by default, by mounting /etc/resolv.conf from the host machine into each container.</li>
<li>Sharing source code or build artifacts between a development environment on the Docker host and a container. For instance, you may mount a Maven target/ directory into a container, and each time you build the Maven project on the Docker host, the container gets access to the rebuilt artifacts.</li>
<li>When the file or directory structure of the Docker host is guaranteed to be consistent with the bind mounts the containers require.</li>
</ul>
<p>Reference:</p>
<p><a href="https://docs.docker.com/storage/">Manage data in Docker</a></p>
]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>技术，Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Init</title>
    <url>/2021/05/06/Init/</url>
    <content><![CDATA[<h1 id="我来了，我看到，我征服。"><a href="#我来了，我看到，我征服。" class="headerlink" title="我来了，我看到，我征服。"></a>我来了，我看到，我征服。</h1><p>这里是宇言博客的第一站，是宇言进入互联网的第一次宣言。</p>
<p>在这里，将记录我学习过程中的所有的感想和困惑。</p>
<p>在这里，将记录我职业人生中所有的得失和心得。</p>
<p>如果你恰巧看到这个网站，希望你有开心的一天。</p>
<p>—— by 宇言</p>
]]></content>
      <categories>
        <category>随笔</category>
      </categories>
      <tags>
        <tag>随笔， 感想</tag>
      </tags>
  </entry>
  <entry>
    <title>初识 Kafka</title>
    <url>/2021/05/07/%E5%88%9D%E8%AF%86Kafka/</url>
    <content><![CDATA[<p>今天花了一下午时间初步了解了一下Kafka。</p>
<p>首先非常感谢朋友 <a href="https://github.com/Ecohnoch">@Ecohnoch</a>  提供的讲解材料和 <a href="git@github.com:Ecohnoch/kafka-python-demo.git">Python Demo</a>。如果读者对Kafka也感兴趣，建议在看完本文之后，也移步 <a href="https://github.com/Ecohnoch/kafka-python-demo">Demo</a>，运行感受一下 Kafka 的消息分发机制。</p>
<p>初识 Kafka 要解决的第一个问题是： <strong>什么是 Kafka？</strong></p>
<h2 id="Kafka的定义及作用"><a href="#Kafka的定义及作用" class="headerlink" title="Kafka的定义及作用"></a>Kafka的定义及作用</h2><p>我们可以首先来参考一下维基百科对 Kafka 的一句话定义。</p>
<blockquote>
<p><strong>Kafka</strong>是由<a href="https://zh.wikipedia.org/wiki/Apache%E8%BD%AF%E4%BB%B6%E5%9F%BA%E9%87%91%E4%BC%9A">Apache软件基金会</a>开发的一个<a href="https://zh.wikipedia.org/wiki/%E5%BC%80%E6%BA%90">开源</a><a href="https://zh.wikipedia.org/wiki/%E6%B5%81%E5%A4%84%E7%90%86">流处理</a>平台，由<a href="https://zh.wikipedia.org/wiki/Scala">Scala</a>和<a href="https://zh.wikipedia.org/wiki/Java">Java</a>编写。该项目的目标是为处理实时数据提供一个统一、高吞吐、低延迟的平台。其持久化层本质上是一个“按照分布式事务日志架构的大规模发布/订阅消息队列”，[<a href="https://zh.wikipedia.org/wiki/Kafka#cite_note-3">3]</a>这使它作为企业级基础设施来处理流式数据非常有价值。</p>
<p>—— From 维基百科</p>
</blockquote>
<p>在这句话中，我们运用初高中学习的语文缩写方法，提取出主谓宾，可以得到：<font color="#dd0000">“ <strong>Kafka 是一个 消息队列</strong>” </font>这样的关键信息。所以，想要理解 kafka 是什么，我们需要先知道什么是 <strong>消息队列</strong>。关于消息队列的定义，推荐一篇在google上查到的<a href="https://cloud.tencent.com/developer/article/1006035">文章</a>，建议先移步阅读，理解消息队列的定义，意义和作用，常用的应用场景和主流的软件（Kaf）。</p>
<p>如果你认真阅读了这篇文章，基本上已经解决了 Kafka 的定义和作用两个关键的问题。</p>
<p>接下来只需要明白 Kafka 中定义的一些概念，即便不知道如何运行 Kafka 和 Python 调用 Kafka 的语法，也已经掌握了 Kafka 的 essence （本质，要素，精髓）了。</p>
<p>未完待续……</p>
<hr>
<h2 id="Kafka-的一些重要概念"><a href="#Kafka-的一些重要概念" class="headerlink" title="Kafka 的一些重要概念"></a>Kafka 的一些重要概念</h2><h2 id="Kafka-的Docker-运行教程"><a href="#Kafka-的Docker-运行教程" class="headerlink" title="Kafka 的Docker 运行教程"></a>Kafka 的Docker 运行教程</h2><h2 id="Kafka-python-的语法介绍"><a href="#Kafka-python-的语法介绍" class="headerlink" title="Kafka-python 的语法介绍"></a>Kafka-python 的语法介绍</h2>]]></content>
      <categories>
        <category>技术</category>
      </categories>
      <tags>
        <tag>技术</tag>
        <tag>架构</tag>
        <tag>Kafka</tag>
        <tag>消息队列</tag>
      </tags>
  </entry>
</search>
